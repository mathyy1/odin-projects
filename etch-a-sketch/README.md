# etch-a-sketch

This is a drawing program that is similar to a real etch-a-sketch. 

The logic behind it is made with divs. The grid size defaults to 16x16, however the user can change up to 100.
After clicking a cell, that cell's opacity goes up slightly until it is fully lit after 10 hovers.
There is also a random color feature the user can enable.